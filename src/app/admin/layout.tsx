
// This layout file is intentionally left blank for now.
// You can use it to wrap the admin pages with specific layouts in the future.
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

    