specVersion: "v1"
serviceId: "administrator"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ranking-fdc"
connectorDirs: ["./default-connector"]
